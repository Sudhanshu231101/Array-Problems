#include<string>
using namespace std;

int main(){
    string s;
    getline(cin,s);
    int i=0;
    while(s[i]!='\0'){
        if(s[i]>=97 && s[i]<=122 ){
            s[i]=s[i]-32;
        }
        i++;
    }
    cout<<s;
    return 0;
}
