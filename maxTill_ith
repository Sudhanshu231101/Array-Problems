#include<iostream>
using namespace std;

void maxTill_i(int a[], int n){
    int arr[n];
    int max=a[0];
    for(int i=0;i<n;i++){
        if(a[i]>max){
            max=a[i];
        }
        arr[i]=max;
    }
    for(int i=0;i<n;i++){
        cout<<arr[i]<<" ";
    }
}

int main(){
    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    maxTill_i(a,n);
    return 0;
}
